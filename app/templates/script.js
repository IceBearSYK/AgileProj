document.getElementById('signupButton').addEventListener('button', function(event) {
    // Prevent the default form submission behavior
    
    // Redirect to the signup page
    location.href = '/signup';
});
